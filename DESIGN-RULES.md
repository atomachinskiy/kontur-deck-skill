# Kontur Deck — дизайн-правила

Концентрат того, что отличает топовую деку от «сойдёт». Выведено на 60+ боевых слайдах
стратсессии УМ (июль 2026). Применяется к обоим путям: HTML→PDF и Google Slides.
Линтер (render.js) ловит механику; этот файл — про решения, которые линтер не поймает.

## Нулевое правило

Качество деки = контент-план × шаблон × проверка глазами. НЕ вёрстка с нуля.
Если хочется изобрести новый layout — сначала спроси себя, какой из 28 шаблонов
(templates.html) ты не смог заполнить и почему. В 9 случаях из 10 проблема в том,
что на слайд запихнули две мысли.

## Контент: до вёрстки

1. **Один слайд = одна мысль.** Заголовок слайда — это вывод, а не тема.
   Плохо: «Retention». Хорошо: «Retention держит 60% маржи сегмента».
2. **Сначала контент-план, потом HTML.** Таблица: номер → шаблон → заголовок →
   контент по зонам. Уже на этом этапе проверяй лимиты знаков из templates.html.
3. **Не влезает — режь текст, а не шрифт.** Уменьшение кегля ниже шаблонного —
   запрещённый приём. Разрешённые: сократить формулировку, выкинуть третьестепенное,
   разделить на два слайда.
4. **Цифры — точные, из фактуры.** Спорную цифру не округляй в удобную сторону,
   а помечай «оценка» или не показывай. Единицы (%, млрд ₽, тыс.) — меньшим кеглем.
5. **Ритм деки:** после 2-3 плотных слайдов (таблица, 6 цифр) давай воздух
   (тезис, цитата, разделитель). Дека из одних таблиц не читается со сцены.

## Композиция

6. **Один ведущий акцент на слайд** (класс acc-* на .slide). Второй цвет допустим
   только в мультикарточках — по одному на карточку. Радуга = слом стиля.
7. **Сетка святая:** контент живёт в поле 96px от краёв (--pad). Номер страницы
   справа снизу, вордмарк слева сверху. Ничего не прилипает к краям, кроме
   full-bleed элементов (accent-block титула, фото).
8. **Иерархия через размер и вес, не через цвет.** Шкала кеглей шаблонов уже
   выстроена (92/60/34/26/22): не вводи промежуточные размеры.
9. **Пустота — это инструмент.** Слайд, заполненный на 60%, выглядит дороже
   слайда, заполненного на 95%. Не «добивай» слайд контентом ради заполнения.
10. **Выравнивание:** всё по левому краю, кроме центровых шаблонов (тезис, цитата,
    финал). Никакого центрирования заголовков «для красоты».

## Хакатон-формат (шаблоны 21-28, для событий и длинных дек)

- **Чип-лейбл** (.chip) — один на слайд, над заголовком, цвет = ведущий акцент
  слайда. Это навигация («Уровень 2», «Живой кейс»), а не украшение. Два чипа
  на слайде или чип без функции = убрать.
- **Крупные скриншоты решают.** Живой продукт показывай через s-shot-split
  (текст слева, скрин за правый край) или s-shot (витрина 70-80% площади).
  Скрин меньше половины слайда не читается со сцены — тогда это не слайд
  со скриншотом, а слайд с текстом.
- **Полноцветные дивайдеры** (s-divider-full) — для дек от ~20 слайдов со сменой
  частей. Цвета частей чередуй (purple → teal → orange → blue), для светлых
  акцентов добавляй dark-ink. В коротких деках хватает обычного s-divider.
- **Обложка s-cover / финал** с арт-панелью — для статусных и внешних дек;
  рабочим внутренним хватает s-title. Арт-SVG из шаблона не перерисовывать.
- **Спикер-слайд** (s-speaker): 2-4 стат-чипа, первый hot. Цифры из фактуры,
  не «для солидности».

## Текст на слайде

11. **Без длинных тире** (—). Дефис, двоеточие или переписать. En-dash только
    в числовых диапазонах (2-3 если набрано дефисом — ок).
12. Русская типографика: кавычки «ёлочки», ₽ после числа с пробелом,
    NBSP после однобуквенных предлогов в заголовках.
13. В заголовках нет точек. В карточках и списках точки есть (это предложения).
14. Полные имена людей (Имя Фамилия), не инициалы.
15. Без канцелярита и англицизмов, где есть русское слово: не «леверидж», а «рычаг».
    Доменные термины (retention, CAC, NRR) — ок, это язык аудитории.
16. Никаких эмодзи на слайдах. Иконки — только фирменные с CDN Контура или
    геометрические примитивы в цвет акцента.

## Перед отдачей (чек-лист, вручную)

- [ ] render.js прошёл без ошибок (overflow, кегль, тире, страницы PDF).
- [ ] КАЖДЫЙ PNG из render/ просмотрен глазами: переносы слов, сироты-предлоги
      в конце строк, налезания, обрезка у правого края.
- [ ] Заголовки читаются как связный рассказ, если листать только их.
- [ ] Цифры сверены с фактурой (не с памятью).
- [ ] Первый и последний слайд — самые сильные (их запомнят).

## Антипаттерны (мгновенный признак слабой деки)

- Стена буллетов: >5 пунктов или пункты по 2-3 строки.
- Заголовок-тема («Анализ сегмента») вместо заголовка-вывода.
- Кегль 14px и мельче — «чтобы влезло».
- 3+ акцентных цвета на одном слайде.
- Таблица 8×10 «для полноты» — на экране её никто не прочтёт.
- Дефолтные тени, градиенты, скругления больше 24px — не наш стиль.
- Слайд «Спасибо за внимание» с пустотой — финал должен звать к действию
  или оставлять контакт.
